# engines.node を install 時に強制する (#210 で導入、#330 で下限を 24 へ)
# サーバ系は native type stripping で .mts を直接実行するため、型注釈を解釈できない
# 版では起動できない。install は通るのに起動時に落ちる = 原因に辿り着けない事故になるので、
# 入口の EBADENGINE で hard fail させる。
engine-strict=true
