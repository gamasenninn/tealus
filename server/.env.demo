# Demo 環境用 server 設定
# `npm run dev:demo` （または `node --env-file=.env.demo src/app.js`）で使用。
# dev server (port 3000, DB=tealus) と同時起動できるよう、port と DB を別にする。
# 他の変数（JWT_SECRET / OPENAI_API_KEY / VAPID 等）は .env から継承される。
PORT=3001
DB_NAME=tealus_demo
