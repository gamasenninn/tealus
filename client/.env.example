# ========================================
# Tealus Client — Environment Variables
# ========================================
#
# v0.1.0 以降、client は build 時 env を持ちません。
# 設定は server/.env と agent-server/.env で行い、
# client は起動時に GET /api/config 経由で取得します。
#
# このため client/.env は通常不要です。
# (このファイルは互換性のため残してあります)
