# 投稿経路と付随処理の地図 (#383)

**状態**: 実測 2026-08-23。**挙動は何も変えていない** —— いまの姿を書き出しただけ。

メッセージを作る場所ごとに、付随する 4 つの処理が付いているかを並べる。
**「無い」が意図なのか忘れなのかを、コードを読まずに引けるようにするのが目的。**

## 0. なぜ表が要るのか

軸が多い (経路 × 付随処理 4 × メッセージ種別) のに地図がどこにも無く、毎回 grep して組み立て直している。
2026-08-22 には同じ質問に対して「socket にしかない」→「4 か所ある」→「検知したい経路には無い」と
**1 日で 3 回結論が変わった。**

## 1. 数え方 —— ファイル単位で数えない

**`INSERT INTO messages` は 18 本 / 10 ファイル** (2026-08-23 実測)。

#383 起票時の表は**ファイル単位の 10 行**だった。`media.mts` だけは同一ファイル内で挙動が違うと
気づかれて 2 行に割れていたが、**同じことが `lineMessageBridge.mts` (6 本) と `bot.mts` (3 本) でも
起きている。** ファイル単位の表は、それ自体が「違いを潰す」形になっていた。

→ **この表は INSERT 文の単位で数える。**

## 2. 表 (2026-08-23 実測)

- ①socket 配信 = `emit('message:new')` — 画面の更新と未読の数え直し
- ②プッシュ通知 = `sendPushToOfflineMembers`
- ③AI 通知 = `fireWebhooks('message.created')` — **これが無いとエージェントが起動しない**
- ④リンクプレビュー = `processLinkPreviews`

| # | 場所 | 何を投稿するか | ① | ② | ③ | ④ |
|---|---|---|---|---|---|---|
| 1 | `socket/handlers/message.mts:103` | 画面から打つ通常メッセージ | ○ | ○ | ○ | ○ |
| 2 | `socket/handlers/call.mts:30` | 通話の開始・終了 (system) | ○ | × | × | × |
| 3 | `routes/media.mts:81` | **通常のメディアアップロード** | ○ | × | **×** | × |
| 4 | `routes/media.mts:269` | メディアの**転送** | ○ | ○ | ○ | × |
| 5 | `routes/bot.mts:114` → `postAsUser` | Bot API のテキスト投稿 | ○ | × | ○ | × |
| 6 | `routes/bot.mts:314` (`/push-image`) | Bot API の画像投稿 | ○ | × | **×** | × |
| 7 | `routes/bot.mts:402` (`/push-file`) | Bot API のファイル投稿 | ○ | × | **×** | × |
| 8 | `routes/voice.mts:86` | 音声メッセージ | ○ | × | ○ | × |
| 9 | `routes/messages.mts:40` | REST の新規メッセージ | × | × | × | × |
| 10 | `routes/stamps.mts:136` | スタンプ (system) | ○ | × | × | × |
| 11 | `routes/stamps.mts:164` | スタンプ (system) | ○ | × | × | × |
| 12 | `routes/members.mts:20` | 入退室 (system) | ○ | × | × | × |
| 13 | `lineMessageBridge.mts:96` | LINE の**テキスト** | ○ | ○ | × | ○ |
| 14 | `lineMessageBridge.mts:167` | LINE の画像 | ○ | × | × | × |
| 15 | `lineMessageBridge.mts:240` | LINE の複数画像 | ○ | × | × | × |
| 16 | `lineMessageBridge.mts:309` | LINE の音声 | ○ | × | × | × |
| 17 | `lineMessageBridge.mts:382` | LINE のファイル | ○ | × | × | × |
| 18 | `lineMessageBridge.mts:447` | LINE の動画 | ○ | × | × | × |
| — | `lineMessageBridge.mts:509` | LINE の位置情報 | (13 に委譲) | | | |
| — | `services/postAsUser.mts:68` | 5 の実体 + ルームトリガー (#382) | ○ | × | ○ | × |

**4 つ揃っているのは 18 本中 1 本 (#1) だけ。**

## 3. 「×」の内訳 —— 意図か、不明か

これが #383 の本体。**同じ「×」でも中身が違う。**

### 3.1 意図であり、理由がコードに書かれているもの

| 場所 | 欠けているもの | 理由 (要約) |
|---|---|---|
| #13〜18 LINE 全般 | ③AI 通知 | 付けると LINE のメッセージで `@cc-*` が dispatch を起動する = 別の挙動変更。`lineMessageBridge.mts:126-` に明記 |
| #14〜18 LINE の非テキスト | ②プッシュ | 実測: 出品写真ルームで image 24.4/日・video 4.3/日 に対し text 2.6/日。3 秒間隔で連投されるので 1 枚ずつ鳴らすと実用にならない。`lineMessageBridge.mts:121-` に明記 |
| #2 / #10〜12 system メッセージ | ③AI 通知 | スタンプ・入退室・通話でエージェントが動くのは誤り |

★ **#14〜18 の②は「まだ直っていない」ではない。** 「画像も要るならまとめて 1 件の仕組みを先に作ること」と
条件付きで書かれている。#383 起票時の表が LINE を 1 行で「②○」と書いていたのは、
**6 本中 1 本 (テキスト) の話を全体に広げていた。**

### 3.2 不明 —— 何も書かれていないもの (第 2 段で判断する)

| 場所 | 欠けているもの | 分かっている影響 |
|---|---|---|
| **#3 通常のメディアアップロード** | ③AI 通知 | 朝礼の動画がここを通る。**エージェントが起動しない**ので、人が毎朝 1 行打っていた (→ #382 の発端) |
| **#6 `/push-image`** | ③AI 通知 | エージェントが MCP で画像を投稿しても、他のエージェントは起きない |
| **#7 `/push-file`** | ③AI 通知 | 同上 |
| #3 / #6 / #7 / #8 | ②プッシュ | #4 (転送) には付いているのに #3 (通常) には無い。同一ファイル内での非対称 |
| #9 REST の新規メッセージ | ①socket 配信 | クライアントは socket 経由で送るのが前提 (docs/05 §4)。REST で送ると画面に出ない |
| #2〜18 のほぼ全部 | ④リンクプレビュー | 付いているのは #1 と #13 だけ |

★ **#3 と #6/#7 は同じ形**: 「メディアを投稿する経路に ③ が無い」。#4 (転送) だけ付いている。

## 4. これで 3 例目、の意味

`lineMessageBridge.mts` のコメントに 2 例が記録されている。

```
リンクプレビュー  socket 経由にしか付いておらず、LINE 経由はずっと素の URL のままだった
プッシュ通知      socket と media にしか付いておらず、LINE 経由は飛んでいなかった
```

**3 例目が §3.2 の #3 (通常アップロードに ③ が無い)。** どれも同じ形で、
**経路が増えたときに、投稿に付随する処理が一緒に増えていなかった。**

## 5. これから

**素直な統合はしない。** 1 つの関数に寄せて全部同じにすると、意図的に外してあるもの (LINE で AI が動かない、
system メッセージで議事録を作りに行かない) まで揃ってしまい、要件が壊れる。

```
第 1 段  1 つの入口 (postAsUser) を作り、経路を 1 本ずつ移す。
        移すときに「いまの穴」を宣言として書く (例: 通常アップロードは「③ は付けない」と明示)
        → 挙動は完全に同じ。変わるのは「無言」が「宣言」になることだけ
第 2 段  宣言を 1 つずつ見て、意図か忘れかを判断する。直すものだけ直す
```

**第 1 段は 1 本だけ済んでいる** —— `postAsUser` (#382 で作った。#5 の Bot API テキスト投稿がこれを通る)。
残り 17 本は未着手。

**この表は手で保守すると腐る。** 第 1 段が全経路で終われば宣言がコードに書かれるので、
表はそこから生成できるようになる。そこが本来の着地点。

## 6. 検証のしかた (次に数え直す人へ)

```bash
grep -rn "INSERT INTO messages" --include=*.mts server/src | sort
```

★ **各 INSERT の周辺を機械で検索するだけでは間違える。** 2026-08-23 に実際にやった:
`call.mts` の INSERT (30 行) の 77 行あとに `sendPushToUser` があり、
**90 行窓の検索が「②が付いている」と誤判定した**。実際は別のハンドラ (`call:start`) で、
かかってくる通話を鳴らすための push だった。**関数の境界を見て確かめること。**
